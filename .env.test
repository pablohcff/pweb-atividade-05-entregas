# Banco de dados exclusivo para testes — nunca aponta para o banco de desenvolvimento
DATABASE_URL="file:../database.test.sqlite"

PORT=3003

# Segredo JWT exclusivo para o ambiente de testes
JWT_SECRET=chave_secreta_testes_minimo_32_caracteres_ok
JWT_EXPIRES_IN=8h

SESSION_SECRET=session-secret-testes
